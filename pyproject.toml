[project]
name = "hydra-code-x"
version = "0.1.0"
description = "HYDRA-CODE X research implementation"
requires-python = ">=3.10"
dependencies = ["torch>=2.6"]

[project.optional-dependencies]
xla = ["torch-xla"]

dev = ["pytest"]
